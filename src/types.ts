export type loginData = {
    email: string;
    password: string;
}

export type SignUpData = {
    email: string;
    password: string;
    confirmPassword: string;
    firstName: string;
    lastName: string;
}